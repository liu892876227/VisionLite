<UserControl x:Class="VisionLite.Vision.UI.Controls.ParameterPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300">
    
    <UserControl.Resources>
        <!-- 参数标签样式 -->
        <Style x:Key="ParameterLabelStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,5,0,2"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#FF333333"/>
        </Style>
        
        <!-- 参数描述样式 -->
        <Style x:Key="ParameterDescriptionStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="#FF666666"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        
        <!-- 数值输入框样式 -->
        <Style x:Key="ParameterTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="24"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- 滑块样式 -->
        <Style x:Key="ParameterSliderStyle" TargetType="Slider">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <!-- 组合框样式 -->
        <Style x:Key="ParameterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <!-- 复选框样式 -->
        <Style x:Key="ParameterCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#FFF0F0F0" BorderBrush="#FFCCCCCC" BorderThickness="0,0,0,1" Padding="10,5">
            <TextBlock x:Name="TitleText" 
                       Text="参数配置" 
                       FontWeight="Bold" 
                       FontSize="14"/>
        </Border>
        
        <!-- 参数列表 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="ParametersPanel" Margin="10">
                <!-- 无参数提示 -->
                <TextBlock x:Name="NoParametersText" 
                           Text="请选择一个算法以配置参数" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           Foreground="Gray"
                           Margin="20"/>
            </StackPanel>
        </ScrollViewer>
        
        <!-- 操作按钮 -->
        <Border Grid.Row="2" Background="#FFF8F8F8" BorderBrush="#FFCCCCCC" BorderThickness="0,1,0,0" Padding="10,5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ResetButton" 
                        Content="重置" 
                        Width="60" 
                        Height="26"
                        Margin="0,0,5,0"
                        Click="ResetButton_Click"/>
                <Button x:Name="ApplyButton" 
                        Content="应用" 
                        Width="60" 
                        Height="26"
                        Click="ApplyButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
    
</UserControl>