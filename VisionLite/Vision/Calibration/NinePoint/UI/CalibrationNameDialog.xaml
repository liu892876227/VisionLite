<Window x:Class="VisionLite.Vision.Calibration.NinePoint.UI.CalibrationNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="标定名称设置" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="标定名称:" FontWeight="Bold"/>
        <TextBox Grid.Row="1" Name="txtCalibrationName" Height="25" Margin="0,5,0,10"
                Text="{Binding CalibrationName, UpdateSourceTrigger=PropertyChanged}"/>
        
        <Label Grid.Row="2" Content="描述信息:" FontWeight="Bold"/>
        <TextBox Grid.Row="3" Name="txtDescription" Height="60" Margin="0,5,0,10"
                TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Name="btnOK" Content="确定" Width="80" Height="30" Margin="5,0" 
                   IsDefault="True" Click="BtnOK_Click"/>
            <Button Name="btnCancel" Content="取消" Width="80" Height="30" Margin="5,0"
                   IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>